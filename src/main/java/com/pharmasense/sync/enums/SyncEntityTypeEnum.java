package com.pharmasense.sync.enums;

/** Entities the offline client caches and needs incremental change notifications for. */
public enum SyncEntityTypeEnum {
    INVENTORY_ITEM,
    INVENTORY_BATCH,
    PATIENT,
    PRESCRIPTION
}
